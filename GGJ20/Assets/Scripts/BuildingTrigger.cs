using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BuildingTrigger : MonoBehaviour
{
   /* private GameManager _gameManager;
    public GameObject BuildingOutline;

    public bool isKeep, isWall, isSpike, isTower;

    private void OnTriggerEnter2D(Collider2D other)
    {
        if (other.tag == "Player")
        {
            if (isKeep && other.tag == "Player")
            {
                Instantiate(_gameManager.Tier1KeepOUTLINE, _gameManager.Keep_SpawnLOCATION);
            }

            if (isWall)
            {
                Instantiate(_gameManager.Tier1WallOUTLINE);
            }

            if (isSpike)
            {
                Instantiate(_gameManager.Tier1SpikeOUTLINE);
            }

            if (isTower)
            {
                Instantiate(_gameManager.Tier1TowerOUTLINE);
            }
        }
    }
    


    private void OnTriggerExit2D(Collider2D other)
    {
        if (other.gameObject.tag == "Player")
        {
            
        }
    }*/
}
